run = "bash -lc 'cd app && (npm ci || npm install) && npx prisma generate && npx prisma migrate deploy && npm run dev:replit'"
entrypoint = "index.js"

[nix]
channel = "stable-25_05"

[agent]
expertMode = true

[[ports]]
localPort = 3000
externalPort = 80
