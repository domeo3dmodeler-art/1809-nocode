'use client';

import React from 'react';

export default function PropertyAssignmentsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      {children}
    </div>
  );
}
